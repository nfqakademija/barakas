{% extends 'base.html.twig' %}

{% block body %}
    <div class="col-md-4">
        <div class="panel">
            <div class="panel-header">
                <p>Keisti bendrabutį</p>
            </div>
            <div class="panel-content">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button type="submit" class="btn btn-success">Keisti bendrabutį</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}